appConfig {
  persistence = ['springData']
  dataBase = ['mysql']
  logging = 'y'
  unitTest = 'y'
  javaMainClass = "com.rico.springdata.SpringDataApplication"
}

dependencies {
    implementation project(':ro-common')
}
