appConfig {
  appName = "ro-springdata"
  persistence = 'springData'
  dataBase = 'mysql'
  logging = 'y'
  javaMainClass = "com.rico.springdata.Application"
}

dependencies {
    implementation project(':common')
}
