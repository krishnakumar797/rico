appConfig {
  persistence = 'hibernate'
  dataBase = 'mysql'
  cache = 'y'
  logging = 'y'
  javaMainClass = "com.rico.hibernate.Application"
}

dependencies {
    implementation project(':common')
}
