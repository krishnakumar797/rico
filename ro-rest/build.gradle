appConfig {
  appName = "ro-rest"
  rest = 'y'
  persistence = ['springData']
  dataBase = ['mysql']
  logging = 'y'
  devTools = 'y'
  beanValidation = 'y'
  javaMainClass = "com.rico.rest.Application"
}
dependencies {
    implementation project(':common')
}
