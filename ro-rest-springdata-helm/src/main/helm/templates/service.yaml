apiVersion: v1
kind: Service
metadata:
  name: {{ include "service.name" . }}
  labels:
    name: {{ include "service.name" . }}
    chart: {{ include "chart.name" . }}
    instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: {{ include "target.port.name" . }}
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ include "target.port.name" . }}
  selector:
      name: {{ include "pod.name" . }}
      instance: {{ .Release.Name }}
      
---

apiVersion: v1
kind: Service
metadata:
  name: {{ include "db.service" . }}
  labels:
    name: {{ include "db.service" . }}
    chart: {{ include "chart.name" . }}
    instance: {{ .Release.Name }}
spec:
  ports:
  - protocol: TCP
    port: {{ .Values.database.port }}
    targetPort: {{ .Values.database.port }}

---

apiVersion: v1
kind: Endpoints
metadata:
  name: {{ include "db.service" . }}
subsets:
  - addresses:
      - ip: {{ .Values.database.host }}
    ports:
      - port: {{ .Values.database.port }}

